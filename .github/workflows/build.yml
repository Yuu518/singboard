name: Build

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-windows:
    name: Build Windows app
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24

      - name: Install pnpm
        run: npm install --global pnpm@11.13.0

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: actions/cache@v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            src-tauri/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('src-tauri/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Read app version
        shell: pwsh
        run: |
          $metadata = cargo metadata `
            --manifest-path src-tauri/Cargo.toml `
            --no-deps `
            --format-version 1 | ConvertFrom-Json
          $app = $metadata.packages |
            Where-Object { $_.name -eq "singboard" } |
            Select-Object -First 1

          if (-not $app) {
            throw "Could not find the singboard package in Cargo metadata"
          }

          "APP_VERSION=$($app.version)" >> $env:GITHUB_ENV

      - name: Build Tauri app
        run: pnpm tauri build

      - name: Create GitHub release
        uses: softprops/action-gh-release@v3.0.2
        with:
          tag_name: v${{ env.APP_VERSION }}
          name: ${{ env.APP_VERSION }}
          generate_release_notes: true
          files: |
            src-tauri/target/release/singboard.exe
            src-tauri/target/release/bundle/**/*
